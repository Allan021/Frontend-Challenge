import { createTheme } from "@mui/material/styles";
export const appTheme = createTheme({
  palette: {
    primary: {
      main: "#12447f",
    },
    secondary: {
      main: "#25b4dc",
    },
  },
});
